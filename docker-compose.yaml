services:
  postgres:
    image: postgres:17-alpine
    container_name: postgres-db-drawerlink
    restart: unless-stopped
    environment:
      POSTGRES_USER: drawerlink
      POSTGRES_PASSWORD: drawerlink
      POSTGRES_DB: drawerlink_pg
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
